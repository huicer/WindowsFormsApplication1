
2018-5-17

对于将Excel中数据导入到MySql中，先采用将所有字段全部设为字符串类型，


2018-5-22

对于数据库的处理：

1. 单位基本信息表，    始终保持为最新的内容，也就是当数据导入时，将该表内容先清空，再导入

2. 月报表全部数据信息，     将导入数据的全部记录保存在该表中，用以做数据记录用

3. 月报表数据信息，    保存 除单位基本信息表中的字段外的字段，并以“组织机构代码”做为一对多关系

2018-5-24

在AllCompanyInfoForm 中初步设想为：

将在“单位基本信息”表中搜索到的内容，通过双击进入这个窗体界面，要将双击的那条记录的"组织机构代码"传递过来。

在"显示企业所有信息"-----AllCompanyInfoForm窗体，

该窗体将显示"组织机构代码"所对应的所有的记录，这些记录，以“月份”字段显示在左侧DataGridView中

